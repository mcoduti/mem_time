<div class="page-header">
  <h1>
    Characters
    <a href="/characters/new" class="btn btn-lg btn-success">New Character</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Language</th>
        <th>Translation</th>
        <th>Tone</th>
        <th>English spelling</th>
        <th>Character</th>
        <th>Actions</th>
      </tr>

      <% @characters.each do |character| %>
      <tr>
        <td><%= character.language %></td>
        <td><%= character.translation %></td>
        <td><%= character.tone %></td>
        <td><%= character.english_spelling %></td>
        <td><%= character.character %></td>
        <td>
          <a href="/characters/<%= character.id %>" class="btn btn-primary">Show</a>
          <a href="/characters/<%= character.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_character/<%= character.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
