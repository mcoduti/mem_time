<div class="page-header">
  <h1>
    Knowledge unlocked:
    <!-- <a href="/attempts/new" class="btn btn-lg btn-success">New Attempt</a> -->
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Character</th>
        <th>Instances</th>
        <th>Percent Correct</th>
      </tr>

      <% @unique_id.each do |row| %>
      <tr>
        <td><%= Character.find_by(:id => row.character_id).character %></td>
        <td><%= Attempt.where(:character_id => row.character_id, :user_id => current_user.id).count %></td>
        <td><%= (Attempt.where(:character_id => row.character_id, :correct => true, :user_id => current_user.id).count.to_f / Attempt.where(:character_id => row.character_id, :user_id => current_user.id).count.to_f * 100).round(2) %>%</td>

      </tr>
      <% end %>
    </table>
  </div>
</div>



<!-- <div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Correct</th>
        <th>User</th>
        <th>Character</th>
        <th>Actions</th>
      </tr>

      <%# @attempts.each do |attempt| %>
      <tr>
        <td><%#= attempt.correct %></td>
        <td><%#= attempt.user.email %></td>
        <td><%#= attempt.character.character %></td>
        <td>
          <a href="/attempts/<%#= attempt.id %>" class="btn btn-primary">Show</a>
          <a href="/attempts/<%#= attempt.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_attempt/<%#= attempt.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <%# end %>
    </table>
  </div>
</div>
 -->
